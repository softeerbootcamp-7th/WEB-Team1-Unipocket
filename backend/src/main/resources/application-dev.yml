# =============================================================================
# Production/Dev Environment Configuration
# 모든 민감 정보는 환경변수로 주입 (기본값 없음 → 누락 시 즉시 에러)
# =============================================================================
server:
  forward-headers-strategy: native

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
  cloud:
    aws:
      region:
        static: ${AWS_REGION}
      s3:
        bucket: ${AWS_S3_BUCKET_NAME}

# JWT Configuration (기본값 없음)
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION}
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION}
  oauth-state-ttl: ${JWT_OAUTH_STATE_TTL}

# OAuth2 Configuration (기본값 없음)
oauth2:
  providers:
    - type: GOOGLE
      client-id: ${GOOGLE_OAUTH_ID}
      client-secret: ${GOOGLE_OAUTH_SECRET}
      redirect-uri: ${GOOGLE_REDIRECT_URI}
      authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
      token-uri: https://oauth2.googleapis.com/token
      user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
      jwks-uri: https://www.googleapis.com/oauth2/v3/certs
      issuer: https://accounts.google.com
      scope: openid profile email
    - type: KAKAO
      client-id: ${KAKAO_OAUTH_ID}
      client-secret: ${KAKAO_OAUTH_SECRET}
      redirect-uri: ${KAKAO_REDIRECT_URI}
      authorization-uri: https://kauth.kakao.com/oauth/authorize
      token-uri: https://kauth.kakao.com/oauth/token
      user-info-uri: https://kapi.kakao.com/v2/user/me
      scope: profile_nickname,profile_image

# Application Configuration (기본값 없음)
app:
  media:
    presigned-get-expiration-seconds: ${MEDIA_GET_EXPIRATION}
    presigned-put-expiration-seconds: ${MEDIA_PUT_EXPIRATION}
    global-prefix: ${MEDIA_GLOBAL_PREFIX:backend}
    temp-expense-prefix: ${TEMP_EXPENSE_MEDIA_PREFIX:temp-expenses}
    travel-image-prefix: ${TRAVEL_IMAGE_MEDIA_PREFIX:travels}
  frontend:
    url: ${FRONTEND_URL}
    allowed-origins: ${FRONTEND_ALLOWED_ORIGINS}
  cookie:
    domain: ${COOKIE_DOMAIN}
    secure: true

# Logging
logging:
  file:
    name: ${LOG_PATH}/application.log
  logback:
    rollingpolicy:
      file-name-pattern: ${LOG_PATH}/archived/%d{yyyy-MM-dd}/application.%d{yyyy-MM-dd}.%i.log.gz
      max-file-size: 10MB
      max-history: 30
      total-size-cap: 1GB
  level:
    root: ${LOG_LEVEL}

# Swagger / OpenAPI
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

# Gemini API Configuration
gemini:
  api:
    key: ${GEMINI_API_KEY}
    model: ${GEMINI_MODEL}
    endpoint: https://generativelanguage.googleapis.com/v1beta

media:
  cleanup:
    cron: ${MEDIA_CLEANUP_CRON}

analysis:
  batch:
    enabled: true
    dispatch-batch-size: 20
    dispatcher:
      fixed-delay-ms: 6000000

exchange:
  scheduler:
    enabled: true
    cron: "0 0 22 * * *"
    zone: UTC

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    distribution:
      percentiles-histogram:
        method.execution.time: true
      percentiles:
        method.execution.time: 0.95,0.99
      slo:
        method.execution.time: 50ms,100ms,200ms,500ms,1s,2s,5s

observability:
  execution-time:
    slow-threshold-ms: 1000
